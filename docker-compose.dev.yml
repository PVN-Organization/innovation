# Local debug: expose temp MinIO ports when using --profile temp-minio
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml --profile temp-minio up -d

services:
  minio:
    ports:
      - "${MINIO_API_HOST_PORT:-9010}:9000"
      - "${MINIO_CONSOLE_HOST_PORT:-9011}:9001"
